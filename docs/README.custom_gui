This is the custom_gui-write-a-block package meant as a guide to building
out-of-tree packages. To use the custom_gui blocks, the Python namespaces
is in 'custom_gui', which is imported as:

    import custom_gui

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(custom_gui)
